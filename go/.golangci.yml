version: "2"

run:
  timeout: 5m

linters:
  enable:
    - misspell
    - unconvert
    - unused
    - staticcheck
    - govet
  disable:
    - errcheck

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0